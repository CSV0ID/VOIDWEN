# Benchmark results

Empty on purpose. No numbers are published here until they are actually measured —
prose-compression estimates from `run_benchmarks.py` on real before/after transcripts,
and model BLEU/chrF from `model/evaluate.py` on the held-out test set.

Do not paste target numbers from the plan here as if they were results.
