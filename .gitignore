node_modules/
__pycache__/
*.pyc
.DS_Store

# Model artifacts and training data (future phases).
# Data is inherently bilingual (CJK source side); it lives on HuggingFace Datasets
# Hub only and is never committed — keeps the repo CJK-free (Core Principle 3.1).
*.onnx
model/data/*.tsv
model/data/*.json
